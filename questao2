<!DOCTYPE html>
<html>
<head>
    <title>JavaScript</title>

    <!-- a) Adicionar informações do autor, palavras-chave e descrição -->
    <meta name="author" content="Esli Janai">
    <meta name="keywords" content="JavaScript">
    <meta name="description" content="Atividade sobre JavaScript">
</head>
<body>
    <script>
        // Função que verifica se o número é positivo, negativo ou zero
        function verificarNumero() {
            var numero = prompt("Digite um número inteiro:");
            numero = parseInt(numero);

            if (isNaN(numero)) {
                alert("Por favor, digite um número válido.");
            } else if (numero > 0) {
                alert("O número é positivo.");
            } else if (numero < 0) {
                alert("O número é negativo.");
            } else {
                alert("O número é zero.");
            }
        }

        verificarNumero(); // Chama a função
    </script>
</body>
</html>
